# Copie para .env.local e preencha ao publicar no Vercel (ou defina como variável
# de ambiente no dashboard do Vercel). Em dev local, deixe comentado — o Vite já
# faz proxy de /api para http://127.0.0.1:8000.

# VITE_API_BASE_URL=https://seu-backend.onrender.com/api
